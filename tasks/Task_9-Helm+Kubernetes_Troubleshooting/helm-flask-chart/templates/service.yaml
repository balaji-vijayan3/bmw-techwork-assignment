apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-flask-chart.fullname" . }}
  labels:
    {{- include "helm-flask-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-flask-chart.selectorLabels" . | nindent 4 }}
